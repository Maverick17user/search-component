import React from "react";
import ReactDOM from "react-dom/client";
import SearchPreview from "../../src/SearchPreview";

ReactDOM.createRoot(document.getElementById("search")).render(
  <SearchPreview />
);
